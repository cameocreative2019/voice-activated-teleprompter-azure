name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4

      - name: Set NODE_ENV to Production
        run: echo "NODE_ENV=production" >> $GITHUB_ENV

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/"
          api_location: "api"
          output_location: "dist"
          app_build_command: "npm run build"
          api_build_command: "npm run build"
          skip_api_build: false

<<<<<<< HEAD
      # Debug step to analyze build output
      - name: Analyze Build Output
        if: always()
        run: |
          echo "=== API Build Analysis ==="
          echo "API node_modules size:"
          du -sh api/node_modules || echo "No api/node_modules found"
          
          echo "API build output:"
          du -sh api/dist || echo "No api/dist found"
          
          echo "Number of files in API dist:"
          find api/dist -type f | wc -l || echo "No api/dist found"
          
          echo "=== Frontend Build Analysis ==="
          echo "Frontend dist size:"
          du -sh dist || echo "No dist found"
          
          echo "Number of files in frontend dist:"
          find dist -type f | wc -l || echo "No dist found"
          
          echo "Largest files in build:"
          find . -type f -exec du -sh {} + | sort -rh | head -n 10
=======
      # Add step to count files and check size in dist and .oryx_prod_node_modules
      - name: Count Files and Check Directory Size
        run: |
          echo "File count in dist:"
          find /github/workspace/dist -type f | wc -l
          
          echo "File count in .oryx_prod_node_modules:"
          find /github/workspace/api/.oryx_prod_node_modules -type f | wc -l
          
          echo "Directory size summary:"
          du -h -d 1 /github/workspace/dist
          du -h -d 1 /github/workspace/api/.oryx_prod_node_modules
>>>>>>> parent of 1b0e018 (misc fixes)

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
